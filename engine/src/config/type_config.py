from engine.src.resource_type import ResourceType
from types import *

type_config = {
    'structure': {
        'player_built': {
            'road': {
                'cost': {ResourceType: IntType}
            }
        }
    }
}
